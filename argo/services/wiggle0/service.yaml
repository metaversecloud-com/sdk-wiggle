apiVersion: v1
kind: Service
metadata:
  name: wiggle0
  labels:
    app.kubernetes.io/name: wiggle0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wiggle0
  ports:
    - name: http
      port: 80
      targetPort: http
